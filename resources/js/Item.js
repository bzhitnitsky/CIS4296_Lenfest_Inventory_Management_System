class Item {
    constructor(type, brand, model, serial, foapal, currentStatus, asOf) {
      this.Type = type;
      this.Brand = brand;
      this.Model = model;
      this.Serial = serial;
      this.FOAPAL = foapal;
      this.CurrentStatus = currentStatus;
      this.AsOf = asOf;
    }
  }